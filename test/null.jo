(= x (cons-c null-c null-c))
(assert (equal data-cons-t (typeof cons-c)))
(assert (equal null-t (typeof null-c)))
(assert (equal x x))

(= null null-c)

(= (null.cons x)
   (cons-c x null-c))

(assert (equal (null.cons null)
               (cons-c null null)))
