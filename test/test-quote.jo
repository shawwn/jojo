(assert (equal "a" (quote a)))
(assert (equal "a" 'a))
(assert (equal "a" (quote "a")))
(assert (equal "a" '"a"))

(assert (equal (quote (a b c))
               (cons-c 'a
                 (cons-c 'b
                   (cons-c 'c
                     null-c)))))

(assert (equal '(a b c)
               (cons-c 'a
                 (cons-c 'b
                   (cons-c 'c
                     null-c)))))

(assert (equal vect-t (type-of (quote [a b c]))))
(assert (equal vect-t (type-of '[a b c])))

(assert (equal dict-t (type-of (quote {a b c d}))))
(assert (equal dict-t (type-of '{a b c d})))

(= d '{a b c d})

(assert (equal d.a "b"))
(assert (equal d.c "d"))
