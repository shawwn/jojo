(= x (cons-c null-c null-c))
(assert (equal data-cons-t (type-of cons-c)))
(assert (equal null-t (type-of null-c)))
(assert (equal x x))

(= null null-c)

(= (null.cons x)
   (cons-c x null-c))

(assert (equal (null.cons null)
               (cons-c null null)))
